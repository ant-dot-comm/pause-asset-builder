Thin rule broken by a centered brand motif — echoes the "— FOR A MINUTE —" line under the logo.

```jsx
<BrushDivider motif="bars" />
<BrushDivider motif="dash" accent="var(--accent-warm)" />
```

Motifs: `bars` (pause bars), `dot`, `dash`. Use between sections, above a quote, or under a kicker. Quiet — don't overuse on one layout.
