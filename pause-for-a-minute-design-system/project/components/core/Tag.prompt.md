Topic chip for episode themes — outline by default, fills when `active`.

```jsx
<Tag>Discipline</Tag>
<Tag active onClick={() => {}}>Jazz</Tag>
```

Pass `onClick` to make it an interactive filter chip. Use sentence-case topic words; group several with flex + gap.
