Single-line text/email field with a warm rounded shell and calm purple focus ring.

```jsx
<Input type="email" placeholder="your@email.com" />
<div style={{ display: 'flex', gap: 'var(--space-3)' }}>
  <Input type="email" placeholder="Email" />
  <Button>Subscribe</Button>
</div>
```

Sizes `sm | md | lg`. Pair with a `Button` for subscribe CTAs.
