Circular host/guest portrait with an optional lavender ring; shows initials when no image.

```jsx
<Avatar src="guest.jpg" name="Maya Okonkwo" size={64} />
<Avatar name="Brandon Commodore" ring={false} />
```

Use the ring for the featured guest/host; drop it (`ring={false}`) in dense lists.
